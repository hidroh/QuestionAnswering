package qa.model;

/**
 * Interface for query term model, which contains metadata for a term in 
 * original query or in query returned by web search
 */
public interface QueryTerm {

	String getText();
}
